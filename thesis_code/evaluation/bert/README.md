# BERTScore outputs

Expected private artifacts include overall, first-query and later-query score arrays for each prompt condition and both baselines. They preserve one score per evaluated query and are therefore not published; aggregate cross-checks are in [`results/saved-score-audit.json`](../../../results/saved-score-audit.json).
