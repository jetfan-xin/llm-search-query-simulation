# BLEU outputs

Expected private artifacts include task-indexed overall, first-query and later-query BLEU arrays for each prompt condition and both baselines. They preserve record-level order and are excluded; aggregate cross-checks are in [`results/saved-score-audit.json`](../../../results/saved-score-audit.json).
