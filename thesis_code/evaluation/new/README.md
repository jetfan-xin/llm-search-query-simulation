# Plotting tables

Expected private CSV files are `actions_{jaccard,bleu,bert}.csv` and `prompt_{jaccard,bleu,bert}.csv`. Rows contain the condition label, query-position group and one metric value; the notebooks use them to produce thesis figures 12 and 13. Original rendered figures are published under [`assets/results/`](../../../assets/results/).
