# Additional utilities

`evaluation_llm.py` and `util.py` are retained in their original `utils/` location, with project-relative path repairs where needed.

The archive also contained `crawler.py`. Its bytes are identical to `utils/crawler.py` in the adjacent third-party WebAgentFSM copy, so it is not redistributed or presented as original thesis work. Corpus preparation expects OCR/page content produced by that surrounding collection workflow; see [`../data/README.md`](../data/README.md).
