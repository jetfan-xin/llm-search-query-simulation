# Baseline 1 corpus files

Expected private files are `task_description.json`, `title.json`, `abstract.json` and `content.json`. Each maps task IDs `1`–`10` to token sequences extracted from the task and collected search/page material. See the parent [baseline description](../../README.md).
