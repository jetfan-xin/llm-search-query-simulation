# Baseline 2 corpus files

Expected private files are `task_description.json`, `title.json`, `abstract.json` and `content.json`. They have the same task-to-token-list shape as baseline 1; this copy is used by the random/uniform term-selection configuration. See the parent [baseline description](../../README.md).
