# Baseline 2 outputs

This directory receives sampled-query JSON plus per-task/per-user parameter-search CSV files for the random/uniform baseline. Files are ignored because they are record-level experiment outputs. See the parent [baseline description](../../README.md).
